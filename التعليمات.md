# تعليمات نشر تطبيق "سلف المقاولين"

هذا التطبيق جاهز بالكامل. باقي عليك خطوتين بس علشان يصير عندك **رابط دائم** يشتغل أونلاين وأوفلاين ويزامن نفسه تلقائيًا بين تليفونك وحاسبتك.

---

## الخطوة 1: إنشاء قاعدة بيانات مجانية (Firebase) — 3 دقائق

1. افتح: https://console.firebase.google.com
2. سجل دخول بحساب Google العادي حقك (مجاني تمامًا، بدون بطاقة ائتمان).
3. اضغط **Add project** (إضافة مشروع) → سمّيه مثلاً `salaf-almoqawleen` → كمّل الخطوات الافتراضية.
4. بعد ما يفتح المشروع، من القائمة الجانبية اختر **Build → Firestore Database**.
5. اضغط **Create database** → اختر **Start in production mode** → اختر أقرب منطقة (مثلاً `europe-west` أو `me-` لو متوفرة) → Enable.
6. بعد ما تنشئ القاعدة، روح لتبويب **Rules** بنفس الصفحة، واستبدل المحتوى بهذا (يسمح لك وحدك تقرأ وتكتب لاحقًا نقدر نضيف حماية بكلمة سر لو تريد):

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /contractor_entries/{doc} {
      allow read, write: if true;
    }
  }
}
```

اضغط **Publish**.

> ملاحظة أمان: هذا الإعداد يخلي أي شخص يعرف رابط تطبيقك يقدر يعدل البيانات. إذا تريد حماية إضافية (كلمة دخول)، قلي وأضيفها بخطوة لاحقة (Firebase Authentication).

7. ارجع لإعدادات المشروع: اضغط أيقونة الترس ⚙️ (أعلى يسار) → **Project settings**.
8. تحت **Your apps**، اضغط أيقونة **</>** (Web) → سمّي التطبيق أي اسم → **Register app**.
9. راح تظهر لك بيانات مثل هذا:

```js
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "salaf-almoqawleen.firebaseapp.com",
  projectId: "salaf-almoqawleen",
  storageBucket: "salaf-almoqawleen.appspot.com",
  messagingSenderId: "...",
  appId: "..."
};
```

10. افتح ملف **firebase-config.js** اللي بنفس مجلد التطبيق، واستبدل القيم بنفس القيم اللي طلعت لك بالضبط. احفظ الملف.

---

## الخطوة 2: نشر التطبيق برابط دائم (GitHub Pages) — 5 دقائق

1. افتح https://github.com وسجل حساب مجاني (لو ما عندك).
2. اضغط **New repository** → سمّيه مثلاً `salaf-almoqawleen` → خله **Public** → Create repository.
3. بصفحة الريبو الفارغة، اضغط **uploading an existing file**.
4. اسحب كل ملفات هذا المجلد (index.html, manifest.json, sw.js, firebase-config.js, ومجلد icons بكل محتواه) وارفعها → Commit changes.
5. روح لـ **Settings** (بالريبو نفسه) → من القائمة الجانبية **Pages**.
6. تحت **Branch** اختر `main` والمجلد `/root` → Save.
7. انتظر دقيقة، بيطلع لك رابط شكله:

```
https://اسم_حسابك.github.io/salaf-almoqawleen/
```

هذا هو **رابطك الدائم**. افتحه بتليفونك وبحاسبتك — نفس الرابط، ونفس البيانات، تتزامن تلقائيًا.

### تثبيت التطبيق كأيقونة على الشاشة الرئيسية
- **بالتليفون (Android/iPhone)**: افتح الرابط بالمتصفح → قائمة المتصفح (⋮ أو مشاركة) → "إضافة إلى الشاشة الرئيسية".
- **بالحاسبة (Chrome)**: بيظهر أيقونة تثبيت ⊕ بشريط العنوان → اضغطها.

---

## كيف يشتغل أوفلاين؟

- أي تعديل تسويه وانت بدون نت ينحفظ محليًا بجهازك فورًا.
- أول ما يرجع النت (بنفس الجهاز أو أي جهاز ثاني)، يصير التزامن تلقائي بالاتجاهين — بدون أي خطوة إضافية منك.
- شارة أعلى التطبيق تبين لك "متصل" أو "بدون نت" بأي لحظة.

---

## لو احتجت تعديل لاحقًا

أي تعديل على شكل أو منطق التطبيق (مثل تغيير القوائم أو إضافة تقارير)، ارجعلي بنفس المحادثة أو محادثة جديدة، وأرسللك نسخة محدّثة من `index.html` بس ترفعها فوق النسخة القديمة بنفس الريبو (Add file → Upload files → استبدال).
